server:
  port: 8802
  
spring:
  profiles:
    active: dev
  application:
    name: '@pom.artifactId@'
  cloud:
    gateway:
      routes:
        - id: user
          uri: localhost:8803
          predicates:
          - Path=/user/**
            
          
      
      